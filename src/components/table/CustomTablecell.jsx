import { TableCell } from "@mui/material";
// eslint-disable-next-line react/prop-types
export default function CustomTablecell({children}){
  return <TableCell sx={{color:"#FFFFFF"}}>{children}</TableCell>
}